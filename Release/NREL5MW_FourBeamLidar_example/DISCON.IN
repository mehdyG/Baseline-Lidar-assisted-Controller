! Input for the master DLL, it contains the name and infut files of the sub-DLLs that will be called by the master DLL (DISCON.dll by default)

!!Do not change line before this line, add additional DLLs after the number of DLL's, each should has a name of DLL and input file name of DLL(.in file),the DLL will be called by the sqeuence in this document. No empty lines between each sub-DLL is allowed
3                   ! NumberofSubDLLs	- The number of sub DLL
"LDP.dll"         ! SubDLLName        = The name of the sub DLL to be called (lidar data processing and Fitch forward rate for control)
"LDP_IN.IN"          ! SubDLLInputName   = The name of the sub DLL input parameter file name
"FFP.dll"         ! SubDLLName        = The name of the sub DLL to be called (lidar data processing and Fitch forward rate for control)
"FFP_IN.IN"          ! SubDLLInputName   = The name of the sub DLL input parameter file name
"ROSCO2d6.dll"         ! SubDLLName        = The name of the sub DLL to be called (lidar data processing and Fitch forward rate for control)
"ROSCO2.IN"          ! SubDLLInputName   = The name of the sub DLL input parameter file name

